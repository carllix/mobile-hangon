<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Debug builds only: allow plain HTTP so the emulator (10.0.2.2) can reach a
         locally running `uvicorn` without HTTPS. Release builds never get this. -->
    <application android:usesCleartextTraffic="true" />

</manifest>
